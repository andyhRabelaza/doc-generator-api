version: "3.9"

services:
  mongo:
    image: mongo:7.0
    container_name: doc-generator-mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db

  redis:
    image: redis:7
    container_name: doc-generator-redis
    ports:
      - "6379:6379"

volumes:
  mongo-data: